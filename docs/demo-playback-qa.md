# Demo Mode playback QA

The desktop `/map?demo=1` view shows the sample-neighbor status, Demo Mode toggle, replay control, and the simulated thank-you celebration clearly. The celebration is labeled “Demo only — no post was saved.”

The narrow 390×844 view keeps the same controls readable. After the responsive positioning refinement, the celebration banner sits safely below the header, wraps its “Demo only — no post was saved” label without clipping, and no longer overlaps the hero heading. The sample-content label and replay controls remain readable.
